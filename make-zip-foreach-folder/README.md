# Make .zip file for each folder

Put `make_zip_foreach_folder.py` on target location.
And run `python make_zip_foreach_folder.py`.

Else if you using Windows.
Target file drop into `RUN_make_zip_foreach_folder.bat`.
